# --- Core numeric / data ---
numpy>=1.26
pandas>=2.1
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.3

# --- Storage ---
SQLAlchemy>=2.0
# Optional: TimescaleDB / PostgreSQL backend (pip install separately if used)
# psycopg2-binary>=2.9

# --- Config / plumbing ---
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0
PyYAML>=6.0
requests>=2.31
rich>=13.0
typer>=0.9

# --- Interfaces ---
# The web app: one process serves the JSON API and the frontend. The frontend
# is plain HTML/CSS/JS with hand-rolled SVG charts, so there is deliberately no
# Node toolchain, no bundler and no CDN dependency here — `mfie serve` is the
# whole deployment.
fastapi>=0.110
uvicorn[standard]>=0.27

# Legacy Streamlit prototype, kept so `mfie dashboard` still runs. Superseded
# by `mfie serve`; safe to drop if you never use it.
streamlit>=1.50
plotly>=5.18
# Optional: Telegram alerts
# python-telegram-bot>=21.0

# NOTE: TA-Lib is deliberately NOT required. All indicators are implemented
# in mfie/technical/indicators.py with numpy/pandas so the project installs
# on Windows without a C toolchain. See README "Why not TA-Lib".
